(setq leetcode-prefer-language "cpp")
(setq leetcode-prefer-sql "postgres")

;; leetcode doesnt allow 3rd party logins. restore sessino from local chrome cookies with pip3 install my_cookies

(provide 'init-lc)
